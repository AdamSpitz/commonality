# Cheap to regenerate — not worth committing
data/users.json
data/statements.json

# Per-simulation run outputs — ephemeral
output/

# data/attesters.json   — committed (stable reference, deterministically generated)
# data/attestations.json — committed (LLM-generated, expensive to reproduce)

node_modules/

dist/
