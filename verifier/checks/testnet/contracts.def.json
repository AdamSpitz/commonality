{
  "id": "testnet.contracts",
  "description": "Guarded deployed testnet contract check: every expected address in deployments/base-sepolia.env is present and has bytecode on the configured chain.",
  "trigger": { "type": "manual" },
  "retention": { "keep": 5, "keepDays": 30 },
  "command": ["node", "checks/testnet/contracts.mjs"],
  "inputs": [
    { "kind": "file", "path": "environments/testnet.json", "as": "testnetConfig" },
    { "kind": "file", "path": "../deployments/base-sepolia.env", "as": "deploymentEnv" },
    { "kind": "params", "data": { "guardPolicy": "Requires COMMONALITY_VERIFIER_ENABLE_TESTNET_SMOKE=1 and COMMONALITY_TESTNET_RPC_URL before probing deployed contract bytecode." } }
  ],
  "timeoutMs": 120000
}
