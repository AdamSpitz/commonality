/** Ref names owned by shared substrate features and unavailable as CauseStarter slugs. */
export const RESERVED_REF_NAMES: ReadonlySet<string> = new Set([
  'created-statements',
  'favorites',
  'bookmarks',
  'draft-post',
]);
