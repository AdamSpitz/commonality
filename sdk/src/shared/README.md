# Shared stuff

This directory is basically for types that are shared between indexer-queries and graphql-queries. (Makes sense - the local graphql resolvers are mostly just calling the indexer's graphql interface and passing through the results.)

(TODO: this comment is obsolete, we got rid of graphql-queries.)