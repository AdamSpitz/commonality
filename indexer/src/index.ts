/**
 * Main Indexer Entry Point
 *
 * Registers Ponder event handlers for all contracts.
 * All handlers live in events-cache — they capture raw events and
 * store raw events. Business logic lives in the SDK.
 */

import "./events-cache";
