/**
 * Events Cache - Raw event capture utilities
 *
 * The captureRawEvent helper has been moved to ../utils/rawEvents.ts
 * and is now called directly from each subsystem handler.
 *
 * This file is kept for reference but no longer registers any ponder.on() handlers.
 */
