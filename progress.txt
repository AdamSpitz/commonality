AI: use this file to write notes to yourself.

High-level stuff is all in order, it's time to implement.

## Current priority (2026-02-01):
Fix E2E test concurrency issue before adding more tests. Tests that create blockchain state fail when run in parallel. Need to configure Playwright to serialize these tests.

## Session (2026-02-01) - E2E Test Concurrency Fix (COMPLETED ✅)

**Task:** Configure Playwright to serialize tests to avoid blockchain state conflicts

**TASK COMPLETED SUCCESSFULLY ✅**

**Problem:**
When running multiple E2E tests in parallel (Playwright default: 2 workers), tests that modify blockchain state interfered with each other:
- IPFS sync contention (multiple statements created simultaneously)
- Indexer processing delays (couldn't keep up with rapid events)
- Tests failed when run together but passed individually

**Solution:**
Updated [ui/playwright.config.ts](ui/playwright.config.ts) to disable parallel execution:
- Set `fullyParallel: false` (was `true`)
- Set `workers: 1` for all environments (was only for CI)
- Added clear comment explaining why serialization is needed

**Rationale:**
E2E tests share the same Docker backend (Hardhat node, IPFS, GraphQL indexer). Tests that create statements or express beliefs modify this shared state and must run serially to avoid race conditions.

**Trade-off:**
- Slower test execution (~1 minute for 11 tests vs potentially faster parallel)
- More reliable tests (no flaky failures from state conflicts)
- Acceptable for current test suite size (can optimize later if needed)

**Files changed:**
- `ui/playwright.config.ts` - Disabled parallel execution, set workers to 1

**Test results:**
All test suites passing ✅:
- SDK tests: 72/72 passing
- Hardhat tests: 243/243 passing
- UI unit tests: 265/265 passing
- **E2E tests: 11/11 passing** (was failing when run in parallel)

**Next steps:**
Can now safely add more E2E tests for belief expression and other workflows without concurrency issues.

## Session (2026-01-31 afternoon) - Wagmi Mock Connector Research (COMPLETED)

**Task:** Research wagmi connector capabilities for private key account support in E2E tests

**Key Findings:**

1. **Wagmi mock connector ONLY accepts addresses, NOT private keys**
   - Parameter type: `accounts: readonly [Address, ...Address[]]`
   - Documentation confirms it takes address strings (0x...), not private key accounts
   - Source: https://wagmi.sh/core/api/connectors/mock

2. **Wagmi does NOT support hoisting private key accounts to wagmiConfig**
   - This is a known limitation discussed in GitHub issue #3480
   - Quote: "Wagmi currently does not support hoisting Private Key & Mnemonic Accounts to the top-level Wagmi Config"
   - Workaround mentioned: "creating a custom connector based on Viem account"
   - Source: https://github.com/wevm/wagmi/discussions/3480

3. **Current implementation already converts private keys correctly**
   - `ui/src/wagmi.ts` createMockConfig() function (lines 33-60):
     - Accepts `addressOrPkey: 0x${string}`
     - Converts private key to account using `privateKeyToAccount(addressOrPkey)`
     - But then ONLY passes the address to mock connector: `mock({ accounts: [address] })`
     - The actual account object with signing capability is discarded!

4. **The root problem:**
   - SDK actions expect `walletClient.account` to be a full viem Account object with signing methods
   - Wagmi's useWalletClient() hook returns a wallet client from the connected connector
   - Mock connector only knows about the address, not the private key
   - Therefore walletClient.account is either undefined or a minimal object without signing capability

5. **Why approach #3 (from previous session) won't work:**
   - The previous session recommended "Update wallet fixture to use private key accounts"
   - But wagmi's mock connector fundamentally doesn't support this
   - Even if we pass the full account object to createMockConfig, we can only pass the address to mock()
   - The wallet client returned by useWalletClient() will still not have signing capability

**Conclusion:**

Approach #3 from the previous session is **NOT viable**. The wagmi mock connector cannot provide a wallet client with private key signing capabilities.

**Recommended new approach:**

We need to create a **custom connector** that wraps a viem account with private key:

1. Create `ui/e2e/fixtures/privateKeyConnector.ts`:
   - Custom wagmi connector that holds a full viem account (from privateKeyToAccount)
   - Implements the Connector interface
   - Provides getProvider(), getAccounts(), etc.
   - Most importantly: makes the full account object available to walletClient

2. Use the custom connector in createMockConfig instead of mock():
   ```typescript
   connectors: [createPrivateKeyConnector(privateKey)]
   ```

3. This matches the workaround mentioned in GitHub discussions: "creating a custom connector based on Viem account"

**Alternative approach (potentially simpler):**

Instead of fighting with wagmi, modify the UI components to NOT use the SDK actions directly. Instead:
- Use wagmi's native hooks: `useWriteContract()`, `useSimulateContract()`, etc.
- These hooks handle signing through the connector properly
- Create a thin adapter layer that translates SDK action patterns to wagmi patterns
- Keep SDK actions as-is for integration tests (which use createTestClients with real accounts)

This second approach is more aligned with wagmi's architecture and may be more maintainable.

**Next steps:**

This research task is complete. The findings show that the problem is architectural and requires either:
1. Building a custom connector (complex, but keeps SDK actions unchanged)
2. Refactoring UI to use wagmi hooks instead of SDK actions (simpler, more idiomatic)

The project lead or next implementer should decide which approach to take.

## Latest session (2026-01-31) - Wagmi Client Configuration Investigation (INCOMPLETE - TASK LARGER THAN EXPECTED)

**Task attempted:** Fix wagmi client configuration to make connected account available for signing transactions

**THIS WAS A LARGER TASK THAN EXPECTED** - Task stopped due to architectural complexity

**Root cause analysis:**
The E2E test failure is due to a fundamental architectural mismatch between:
1. The SDK (`@commonality/sdk`) - designed for test environments with direct private key access
2. The UI - using wagmi which abstracts away private keys and uses browser wallets

**The problem in detail:**

The SDK's actions (like `believeStatement`, `createAndSignStatement`) expect a `TestClients` object with:
```typescript
{
  walletClient: WalletClient,  // Must have .account property for signing
  publicClient: PublicClient,
  account: Address
}
```

In test/integration environments, the wallet client is created with `createWalletClient({ account: privateKeyToAccount(privateKey), ... })`, which gives it a full viem account object with signing capabilities.

In the UI using wagmi:
- `useWalletClient()` returns a wallet client from the connected browser wallet
- wagmi's mock connector (used in E2E tests) doesn't support the same signing methods
- Error progression:
  1. First: "Could not find an Account" - walletClient.account was undefined
  2. After adding `{ address }`: "Account type undefined is not supported" - account missing `type` property
  3. After adding `{ address, type: 'json-rpc' }`: "method eth_sendTransaction not supported" - json-rpc accounts expect RPC-level signing, not available in mock connector

**Why this is a larger task:**

Fixing this properly requires ONE of these approaches:

1. **Refactor the SDK** to not require `walletClient.account` for signing
   - Make SDK actions use wagmi's hooks directly (useWriteContract, etc.)
   - This is a significant SDK refactoring affecting all actions

2. **Create a UI-specific wrapper layer** around SDK actions
   - Wrapper translates between wagmi patterns and SDK expectations
   - Maintains SDK as-is for integration tests
   - Requires designing and implementing the wrapper API

3. **Make E2E tests use real wallet simulation** with private keys
   - Use wagmi's `privateKeyToAccount` to create proper accounts in E2E tests
   - This might require changes to how wagmi's mock connector works
   - May need to inject private key into browser context (security implications)

**Recommendation for breaking down the task:**

The smallest viable approach is #3 - modify E2E test wallet setup:

1. **First subtask:** Research wagmi connector capabilities
   - Can wagmi's mock connector accept a private key account instead of just an address?
   - Check wagmi documentation and source code

2. **Second subtask:** Update wallet fixture to use private key accounts
   - Modify `ui/e2e/fixtures/wallet.ts` to create accounts from private keys
   - Pass the full account object to wagmi config, not just the address

3. **Third subtask:** Ensure walletClient has proper account in UI components
   - The walletClient from wagmi should then have a proper account object
   - Test that transactions work in E2E environment

4. **Future consideration:** For production UI (not E2E tests)
   - Real browser wallets (MetaMask, WalletConnect) handle signing differently
   - May need separate code path for production vs E2E
   - OR: Refactor SDK to be wallet-agnostic (approach #1 above)

**Files involved:**
- `ui/src/conceptspace/components/CreateStatementForm.tsx` - attempted fix here
- `ui/src/conceptspace/components/BeliefControls.tsx` - attempted fix here
- `ui/e2e/fixtures/wallet.ts` - should be modified for proper account injection
- `ui/src/wagmi.ts` - mock config creation
- `sdk/src/actions/conceptspace-actions.ts` - uses walletClient.account!

**Changes reverted:** All attempted changes reverted, clean slate for next implementer

**Test results:**
- SDK tests: 72/72 passing ✅
- Hardhat tests: 243/243 passing ✅
- Integration tests: 102/102 passing ✅
- UI unit tests: 265/265 passing ✅
- E2E tests: 10/11 passing (statement creation still failing with account/signing issue)

**Notes for next implementer:**
- Don't try to patch the account object in UI components - that's treating symptoms not cause
- The fix needs to be in how E2E tests set up the wagmi mock connector
- Look at `ui/e2e/fixtures/wallet.ts` and `ui/src/wagmi.ts` createMockConfig
- The mock connector needs to use a proper viem account created from a private key
- Integration tests show the pattern: `privateKeyToAccount(privateKey)` creates proper accounts
All conceptspace UI unit tests are now complete (265 tests across all components and pages).
E2E testing infrastructure is now set up with Playwright.
Docker integration for E2E tests is now complete - Playwright can start/stop backend services automatically.
Wallet connection testing is now working - E2E tests can inject Hardhat accounts via wagmi mock connector.
Next priority: Write E2E tests for critical workflows (statement creation, belief expression, etc.).

## Session (2026-01-31 evening) - Wagmi Hooks Proof-of-Concept Attempt (INCOMPLETE - TASK LARGER THAN EXPECTED)

**Task attempted:** Create a minimal proof-of-concept using wagmi's useWriteContract hook for statement creation

**THIS WAS A LARGER TASK THAN EXPECTED** - Task stopped due to test maintenance burden

**What I tried:**

Attempted to refactor `CreateStatementForm.tsx` to use wagmi's native hooks instead of SDK actions:
- Replace `createAndSignStatement` SDK action with direct `useWriteContract` and `useWaitForTransactionReceipt` hooks
- Keep IPFS upload using SDK's `publishDocument` (doesn't require signing)
- Use wagmi's `writeContract` for the `setBelief` transaction (works with mock connector)

**Why this became larger than expected:**

1. **Extensive unit test coverage:** The component has 265 comprehensive unit tests
2. **Tests are tightly coupled to SDK actions:** Tests mock `createAndSignStatement` and verify its exact calling patterns
3. **Refactoring ripple effect:** Changing the implementation requires rewriting most of the test suite:
   - Mock setup changes (replace `createAndSignStatement` with wagmi hooks)
   - Test assertions changes (verify `useWriteContract` calls instead of SDK action calls)
   - Callback behavior changes (SDK callbacks vs wagmi hook states)
   - Loading state logic changes (useEffect-based vs SDK promise-based)

4. **Not a minimal change:** What started as a "proof of concept" turned into a full refactoring of both component and tests

**The fundamental problem:**

The UI components are architected around SDK actions, but wagmi connectors (especially mock) work differently:
- SDK actions expect a wallet client with `account` property for signing
- Wagmi hooks handle signing internally through connectors
- This architectural mismatch affects not just the code but the entire test suite

**Recommended approach (revised):**

Instead of refactoring existing components, consider creating **a custom wagmi connector** for E2E tests:

1. **Create `ui/e2e/fixtures/privateKeyConnector.ts`:**
   - Custom connector that wraps a viem account with full signing capabilities
   - Implements wagmi's Connector interface
   - Returns a wallet client that has the proper `account` object the SDK expects

2. **Benefits:**
   - NO changes to UI components needed
   - NO changes to unit tests needed
   - NO changes to SDK needed
   - Minimal, focused change (one new file)
   - Keeps production code separate from test infrastructure

3. **Implementation sketch:**
   ```typescript
   // Connector that makes a private key account available to walletClient
   class PrivateKeyConnector extends Connector {
     constructor(private account: PrivateKeyAccount) { ... }

     async getProvider() {
       // Return a provider that can sign with the private key
     }

     async getAccounts() {
       return [this.account.address]
     }

     // Key: Make account available to walletClient
     async getWalletClient() {
       return createWalletClient({
         account: this.account,  // Full account with signing!
         ...
       })
     }
   }
   ```

4. **Why this is better:**
   - Smallest possible change
   - Isolated to test infrastructure
   - Production UI remains idiomatic wagmi
   - SDK remains unchanged
   - All existing tests pass without modification

**Files that would need changes (custom connector approach):**
- `ui/e2e/fixtures/privateKeyConnector.ts` (NEW - ~50-100 lines)
- `ui/src/wagmi.ts` - Update `createMockConfig` to use custom connector
- `ui/e2e/fixtures/wallet.ts` - Minimal updates to pass private key

**Changes reverted:** All attempted changes to CreateStatementForm.tsx reverted, clean slate for next implementer

**Recommendation for next session:**

Try the custom connector approach instead. It's truly minimal and doesn't require touching production code or tests.

## Session (2026-01-31 evening #2) - Custom PrivateKeyConnector Implementation Attempt (INCOMPLETE - TASK LARGER THAN EXPECTED)

**Task attempted:** Create `ui/e2e/fixtures/privateKeyConnector.ts` - a custom wagmi connector for E2E tests with private key signing

**THIS WAS A LARGER TASK THAN EXPECTED** - Task stopped after extensive exploration revealed complexity

**What I discovered:**

After thoroughly exploring the wagmi connector architecture:

1. **Connector interface complexity:**
   - Wagmi connectors use the `CreateConnectorFn` pattern from `@wagmi/core`
   - Must implement: setup(), connect(), disconnect(), getAccounts(), getChainId(), getProvider(), isAuthorized(), switchChain()
   - Must handle event emitters for change/connect/disconnect events
   - Must implement proper onAccountsChanged, onChainChanged, onDisconnect handlers

2. **Provider implementation requirements:**
   - The `getProvider()` method must return an EIP-1193 compatible provider
   - This provider must implement a custom `request` function that handles ALL wallet RPC methods
   - Key methods needed: eth_requestAccounts, eth_accounts, eth_chainId, eth_sendTransaction, personal_sign, eth_signTypedData_v4, wallet_switchEthereumChain
   - The mock connector implementation is ~250 lines just for the provider logic (ui/node_modules/@wagmi/core/dist/esm/connectors/mock.js)

3. **The core challenge:**
   - For signing to work with a private key, the provider's eth_sendTransaction handler must:
     - Accept transaction parameters
     - Sign the transaction locally using the private key account
     - Send the signed transaction to the RPC endpoint
     - Return the transaction hash
   - This requires understanding viem's account signing internals and properly bridging them to the EIP-1193 provider interface

4. **Alternative that might exist but wasn't found:**
   - Checked if wagmi has a built-in connector for private key accounts - none found
   - Checked if viem provides helper functions for EIP-1193 provider creation with accounts - didn't find clear examples
   - The viem `custom()` transport creates a provider, but it's designed to wrap existing providers, not create signing providers from scratch

**Why this is larger than expected:**

This isn't a "50-100 line" custom connector. It requires:
- Deep understanding of EIP-1193 provider spec
- Implementing complex RPC method handling logic
- Properly integrating viem account signing with RPC requests
- Handling edge cases for different transaction types, signing methods, etc.
- Thorough testing to ensure it works with all SDK actions

Estimated complexity: ~200-300 lines for a robust implementation

**Recommended approach for breaking this down:**

Instead of building a full custom connector from scratch, consider these smaller alternatives:

1. **Option A: Modify SDK to accept wagmi hooks (SIMPLEST for E2E tests)**
   - Create a small adapter layer that translates wagmi hook patterns to SDK patterns
   - E2E tests would only need this adapter, not changes to production code
   - Example: `createWagmiClients(useWalletClient(), usePublicClient())` helper
   - Adapter could handle the account mismatch by using wagmi's native signing methods

2. **Option B: Use viem's test client in E2E environment (CLEANEST separation)**
   - E2E tests bypass the UI components' SDK calls entirely
   - Instead, E2E tests directly call contracts using viem test clients (like integration tests do)
   - UI components continue to use wagmi, but we don't test the signing flow in E2E
   - We verify UI behavior (forms, validation) separately from blockchain integration

3. **Option C: Find/adapt an existing connector implementation**
   - Search npm for wagmi connectors that support private keys
   - Adapt the mock connector code specifically for private key signing (fork and modify)
   - This at least gives us a starting point rather than building from scratch

**Files explored:**
- ui/node_modules/@wagmi/core/dist/types/connectors/createConnector.d.ts - Connector interface
- ui/node_modules/@wagmi/core/dist/esm/connectors/mock.js - Mock connector implementation (~250 lines)
- ui/src/wagmi.ts - Current mock config setup
- ui/e2e/fixtures/wallet.ts - Wallet fixture
- sdk/src/actions/common.ts - How createTestClients() works
- sdk/src/actions/conceptspace-actions.ts - How SDK actions use walletClient.account

**Changes made:** None - task stopped during exploration phase

**Recommendation for project lead:**

This E2E testing blocker requires an architectural decision:
- Do we invest ~4-8 hours building a robust custom connector?
- Do we change how UI components interact with the SDK (adapter layer)?
- Do we change our E2E testing strategy (bypass wagmi, test contract calls directly)?

Current approach (custom connector) is more complex than initially estimated.

(Note from user: let's change our E2E testing strategy. Bypass wagmi, test contract calls directly.)
## Session (2026-01-31 evening #3) - E2E Testing Strategy Refactor (PARTIALLY COMPLETE)

**Task:** Refactor E2E tests to bypass wagmi and test contract calls directly (per user directive)

**CORE TASK COMPLETED** ✅ - SDK calls now work in E2E tests, bypassing wagmi's mock connector

**What was accomplished:**

1. **Created E2E blockchain utilities** (`ui/e2e/utils/blockchain.ts`):
   - `createE2ETestClients()` - Creates viem test clients for E2E tests
   - `getContractAddresses()` - Loads contract addresses from ui/.env
   - Properly handles ESM imports for Playwright environment

2. **Refactored statement creation E2E test**:
   - Test now uses viem test clients directly (like integration tests)
   - Calls `createAndSignStatement()` SDK action with real private key account
   - Successfully creates statements: IPFS upload ✅, tx signing ✅, list update ✅
   - Test output confirms all blockchain operations work:
     ```
     Statement uploaded to IPFS: bafkreiepkagwicfqalacou7jviuzkiryeto7d463c5pchnh6dmfyavjqcm
     Statement signed, tx hash: 0x17937ef7510013a85d5dbc367582ae71f09aac2e79b1494592961f1cb4a8e857
     Created list updated, tx hash: 0x9829ff857cb96d38c1b4ce68d3b3a7f87581d605520e8f48b3bd399dfa921b51
     ```

**The original blocker is RESOLVED:**
- wagmi's mock connector signing issue: BYPASSED ✅
- E2E tests can now create transactions using viem test clients ✅
- SDK actions work correctly in E2E environment ✅

**NEW ISSUE DISCOVERED (separate from original task):**

After creating the statement successfully, it doesn't appear on the browse page. This is NOT a wagmi/signing issue - it's either:
1. GraphQL indexer not syncing properly in E2E environment
2. UI browse page query logic bug
3. Statement content not being fetched from IPFS

Symptoms:
- Statement is created on-chain successfully (confirmed by tx hash)
- IPFS upload works (confirmed by CID)
- Browse page shows "Untitled Statement" but not the newly created statement
- Timeout waiting for statement text to appear on page

**This is a DIFFERENT TASK** - not "bypass wagmi for signing" but "debug browse page GraphQL queries"

**Recommendation:**

The original task (bypass wagmi for transaction signing) is COMPLETE. The E2E test infrastructure now works correctly for direct blockchain interactions.

The browse page display issue should be treated as a separate task:
- Option 1: Debug why GraphQL indexer isn't syncing the statement
- Option 2: Debug why browse page query isn't finding the statement  
- Option 3: Check if IPFS content fetching is working in E2E environment

**Files changed:**
- `ui/e2e/utils/blockchain.ts` (NEW) - E2E test utilities for viem clients
- `ui/e2e/statement-creation.spec.ts` - Refactored to use direct SDK calls

**Test results:**
- 10/11 E2E tests passing ✅
- 1 test failing: statement creation end-to-end (blockchain part works, UI display doesn't)

**Next steps for follow-up task:**
1. Investigate GraphQL indexer sync in E2E environment
2. Check if browse page is querying correctly
3. Verify IPFS content fetching works in E2E tests


## Session (2026-01-31 evening #4) - E2E Browse Page Display Bug (INCOMPLETE - TASK LARGER THAN EXPECTED)

**Task attempted:** Debug why created statements don't appear on browse page in E2E tests

**THIS WAS A LARGER TASK THAN EXPECTED** - Task stopped after discovering root cause requires architecture changes

**Root cause identified:**

The problem is an **IPFS configuration mismatch** between test environment components:

1. **E2E test (host machine):**
   - Uses SDK's `createAndSignStatement()` action
   - SDK uploads to IPFS via `IPFS_API` environment variable
   - In E2E tests, SDK likely falls back to **mock IPFS** (in-memory) because `IPFS_API` env var isn't set in the Playwright test process
   - Mock IPFS stores content in memory only - not accessible to indexer

2. **Indexer (Docker container):**
   - Fetches from IPFS gateway via `IPFS_GATEWAY` environment variable  
   - Set to `http://ipfs:8080/ipfs` (local IPFS node in Docker network)
   - Tries to fetch the CID from real IPFS node
   - **Fails because content was stored in SDK's mock IPFS, not the real node**

**Evidence:**
- Test output shows: `IPFS sync result: { success: true, message: 'IPFS sync completed', syncedCount: 0, failedCount: 1 }`
- This means indexer tried to fetch IPFS content but it wasn't available
- SDK uploaded to mock IPFS (in-memory), but indexer tried to fetch from real IPFS node

**Why this is larger than expected:**

Fixing this requires coordinating IPFS configuration across multiple components:

1. **Playwright test process needs IPFS_API environment variable**
   - Must be set to `http://localhost:5001` so SDK uploads to real IPFS
   - Playwright config doesn't currently set this env var

2. **Alternative: SDK needs to respect VITE_IPFS_API**
   - VITE_ prefix makes it available in browser context
   - But E2E tests run in Node.js (Playwright), not browser

3. **Alternative: Configure global-setup.ts to set process.env**
   - Set `process.env.IPFS_API = 'http://localhost:5001'` in global-setup
   - Make it available to all test processes

4. **Verify end-to-end:**
   - SDK uploads to real IPFS at localhost:5001 ✓
   - Content is stored in Docker IPFS container (ports mapped 5001:5001, 8080:8080) ✓
   - Indexer fetches from http://ipfs:8080/ipfs (inside Docker network) ✓
   - All three need to point to the same IPFS instance

**Recommended approach for breaking down the task:**

**Option A (Simplest):** Set IPFS_API in Playwright test environment
1. Update `ui/e2e/utils/blockchain.ts` or test file to set `process.env.IPFS_API = 'http://localhost:5001'`
2. Verify SDK actually uses this when creating statements in E2E context
3. Test that content is fetchable from both localhost:8080 (host) and ipfs:8080 (Docker)

**Option B:** Set in Playwright config
1. Update `ui/playwright.config.ts` to set environment variables for all tests
2. Add `IPFS_API: 'http://localhost:5001'` to the env config
3. Restart tests to pick up the env var

**Option C:** Set in global-setup.ts
1. Update `ui/e2e/global-setup.ts` to export environment variables
2. Make IPFS_API available to test processes
3. This matches the pattern used for contract addresses

**Files involved:**
- `ui/e2e/statement-creation.spec.ts` - E2E test that creates statements
- `ui/e2e/utils/blockchain.ts` - Could set env var here
- `ui/playwright.config.ts` - Could set env vars for all tests
- `ui/e2e/global-setup.ts` - Could export IPFS_API
- `sdk/src/actions/common.ts` - Checks for IPFS_API or VITE_IPFS_API env vars
- `indexer/src/conceptspace/utils/ipfs.ts` - Fetches from IPFS_GATEWAY

**Changes made so far:**
1. Added manual IPFS sync trigger endpoint to indexer API ✅
   - `POST /conceptspace/api/sync-ipfs`
   - Calls `runIpfsSyncIteration()` on demand
2. Updated E2E test to call sync endpoint after statement creation ✅
   - But sync failed because content not in real IPFS

**Changes should be reverted:** 
- Keep the manual sync endpoint (it's useful for debugging)
- Keep the E2E test calling it (it will work once IPFS config is fixed)

**Recommendation for next session:**

Try Option A first (simplest, most surgical):
1. At the top of `ui/e2e/statement-creation.spec.ts`, add: `process.env.IPFS_API = 'http://localhost:5001'`
2. Run the test and verify SDK uploads to real IPFS instead of mock
3. Verify indexer can fetch the content
4. If this works, move the env var setting to a better location (utils or config)

If Option A doesn't work (e.g., SDK already has the env var set but there's a different issue), then investigate why IPFS upload/fetch isn't working between Docker and host.

## Session (2026-01-31 evening #5) - IPFS Configuration Fix (COMPLETED ✅)

**Task:** Fix IPFS configuration for E2E tests by setting IPFS_API environment variable

**TASK COMPLETED SUCCESSFULLY ✅**

**Solution implemented:**

Set `process.env.IPFS_API = 'http://localhost:5001'` at the top of `ui/e2e/utils/blockchain.ts` before importing the SDK. This ensures the SDK uses the real IPFS node instead of mock IPFS when E2E tests call SDK actions.

**Why this works:**

1. **SDK behavior:** The SDK's `uploadToIPFS()` function checks for `IPFS_API` or `VITE_IPFS_API` environment variables. If neither is set, it falls back to mock (in-memory) IPFS.

2. **Previous problem:** E2E tests ran in Node.js (Playwright) and didn't have `IPFS_API` set, so SDK used mock IPFS. The indexer tried to fetch from the real IPFS node and couldn't find the content.

3. **Fix:** Setting `process.env.IPFS_API` before SDK import ensures all E2E test SDK calls use the real IPFS node at `http://localhost:5001`, which is the same IPFS instance the indexer uses.

**Files changed:**

1. `ui/e2e/utils/blockchain.ts` - Added IPFS_API environment variable configuration
2. `indexer/src/api/conceptspace-api.ts` - Fixed TypeScript error for regex match groups (unrelated but needed for build to pass)

**Test results:**

All test suites passing ✅:
- SDK tests: 72/72 passing
- Hardhat tests: 243/243 passing
- Integration tests: 102/102 passing
- UI unit tests: 265/265 passing
- **E2E tests: 11/11 passing** (was 10/11 before fix)

**E2E test output confirms fix:**

```
Statement uploaded to IPFS: QmYu1vhZr3hJ9zLD4HaioWTNM9PfpXKmj8A7vGUk9hAxXr
Statement signed, tx hash: 0x6f68a1fb89723372b405b506452e37162eb47f4260926592f423137fff1c79a7
Created list updated, tx hash: 0xf74487f5f941019a9d4aef676a15b19e909cab7728dac4e59b2e82dbd4f84bb3
IPFS sync result: { success: true, message: 'IPFS sync completed', syncedCount: 1, failedCount: 0 }
Statement found on browse page!
```

**Key success metrics:**
- IPFS sync now shows `syncedCount: 1, failedCount: 0` (was `syncedCount: 0, failedCount: 1`)
- Statement successfully appears on browse page
- Full end-to-end workflow working: IPFS upload → blockchain transactions → indexer sync → UI display

**Recommendation:**

The E2E testing infrastructure blocker is now fully resolved. All 11 E2E tests pass. Next steps from [TODO.md](TODO.md):
1. Write additional E2E tests for critical workflows (belief expression, other features)
2. Continue with conceptspace UI implementation
3. Eventually: generative testing, AI attester service, deployment

## Session (2026-01-31 evening #6) - Belief Expression E2E Test (INCOMPLETE - TASK LARGER THAN EXPECTED)

**Task attempted:** Write E2E test for belief expression workflow

**THIS WAS A LARGER TASK THAN EXPECTED** - Task stopped due to test concurrency issues

**What I attempted:**

Created `ui/e2e/belief-expression.spec.ts` with three tests:
1. Express belief in a statement and verify UI updates
2. Express disbelief in a statement and verify UI updates
3. Verify GraphQL query returns correct belief state

Each test followed the same pattern as `statement-creation.spec.ts`:
- Create a statement using SDK (bypass wagmi)
- Express belief/disbelief using SDK
- Trigger manual IPFS sync
- Navigate to statement page
- Verify UI shows correct belief state and counts

**The problem discovered:**

When running tests individually (e.g., `npx playwright test statement-creation.spec.ts`):
- ✅ Tests pass (2/2 passed in 25.8s)
- ✅ IPFS sync works correctly (syncedCount: 1, failedCount: 0)
- ✅ Statements appear on UI

When running all E2E tests together (including the new belief expression tests):
- ❌ Multiple tests fail (4/14 failed)
- ❌ Statements don't appear on browse/detail pages
- ❌ Tests timeout waiting for statement content

**Root cause:**

Running multiple statement-creation tests concurrently causes issues:
1. **IPFS sync contention:** Multiple tests creating statements simultaneously may overwhelm the IPFS sync endpoint or indexer
2. **Indexer processing delays:** The indexer might not keep up with rapid blockchain events from parallel tests
3. **Test isolation:** Tests aren't properly isolated - they share the same Docker backend (Hardhat, IPFS, indexer)

**Why this is larger than expected:**

The original task ("write E2E test for belief expression") seemed straightforward, but it revealed an architectural issue with the E2E test infrastructure:

1. **Need for test serialization:** Tests that create blockchain state probably need to run sequentially, not in parallel
   - Playwright runs tests in parallel by default (2 workers)
   - Statement-creation tests interfere with each other when run concurrently

2. **Need for better test isolation strategies:**
   - Option A: Configure Playwright to run statement-creation tests serially (`fullyParallel: false` or `workers: 1`)
   - Option B: Add longer wait times and retry logic for indexer sync
   - Option C: Use test fixtures to ensure each test gets isolated blockchain state
   - Option D: Use different accounts + test-specific tags/filters to avoid conflicts

3. **Need to update existing tests:** Even `statement-creation.spec.ts` fails when run with other similar tests

**Recommended approach for breaking down the task:**

**First subtask: Fix test concurrency**
1. Update `ui/playwright.config.ts` to serialize tests that modify blockchain state
   - Use `test.describe.serial()` for statement-creation tests
   - OR: Configure specific test files to run with `workers: 1`
2. Verify existing `statement-creation.spec.ts` passes when run with full test suite
3. Document the concurrency constraint

**Second subtask: Add belief expression tests**
1. After fixing concurrency, add the belief expression test file
2. Use `test.describe.serial()` to ensure tests run one at a time
3. OR: Create test helpers that handle proper synchronization/waiting

**Alternative simpler approach:**

Just configure Playwright to run all E2E tests serially (workers: 1):
- Simplest fix
- Slower test execution (~30s per test = ~5 minutes for 10 tests)
- But more reliable for tests that modify shared blockchain state
- Can optimize later if test suite grows large

**Files involved:**
- `ui/playwright.config.ts` - Need to configure test serialization
- `ui/e2e/belief-expression.spec.ts` - New test file (created but removed)
- `ui/e2e/statement-creation.spec.ts` - Existing test (works alone, fails with others)

**Changes reverted:**
- Removed `ui/e2e/belief-expression.spec.ts`
- All other files unchanged

**Test results:**
- Running statement-creation alone: 2/2 passing ✅
- Running all tests together: 4/14 failing (including statement-creation) ❌

**Recommendation for next session:**

1. **Quick fix:** Update `playwright.config.ts` to set `workers: 1` or use `fullyParallel: false` for E2E tests
2. **Verify:** Run full E2E suite and confirm all 11 existing tests still pass
3. **Then:** Add the belief expression tests (they should work once concurrency is fixed)

This is a smaller, more focused task that addresses the root cause before adding more tests.

