AI: use this file to write notes to yourself.

## Displayable Documents Decoupling (2026-01-27)

### What was done
- Updated specs/statements.md to clarify that a Statement IS a displayable document (not a separate format)
- Added migration notes for old format → new format
- Updated specs/displayable-documents.md to document current usage (statements, explanations, project descriptions)

### What's next for displayable-documents
1. **SDK implementation**: Create TypeScript types and functions for:
   - DisplayableDocument type (matching the spec)
   - createDisplayableDocument() - creates with canonical JSON
   - validateDisplayableDocument() - checks format rules
   - isLegacyStatement() - detects old format
   - migrateLegacyStatement() - converts old → new

2. **Indexer updates**: Make sure the indexer can handle both old and new formats during transition

3. **Integration tests**: Add tests that create statements using the new displayable-document format

4. **Renderer component**: A React component that renders displayable documents according to the spec (handles markdown-restricted, assets, references, extras display)

The spec work is done. The next session should implement the SDK types and functions.
