AI: use this file to write notes to yourself.

## Current status

The AlignmentAttestations refactoring is complete:
- Contract renamed from ProjectAlignment to AlignmentAttestations
- Event renamed from ProjectAlignmentAttestation to AlignmentAttestation
- Parameter renamed from projectAddress to subjectAddress
- topicStatementId is now required (non-zero)
- All code updated (contract, tests, indexer, SDK, integration tests)
- All documentation updated (specs/README.md, fundingportals.md, indexers.md, legal.md, decoupling.md)
- See specs/alignment-attestations-refactoring.md for full checklist (all items marked complete except fake-data-generation which isn't actively maintained)

All feedback loops pass:
- 24 hardhat AlignmentAttestations tests pass
- 102 integration tests pass
- Indexer type check passes
- SDK builds successfully

## Third-party secondary marketplace research (Jan 2026)

Researched whether Seaport, SudoSwap, Rarible, or Reservoir could replace our ERC1155SecondaryMarket.
Conclusion: keep our custom contract. Details written up in specs/legal.md.
Key reasons:
- Seaport/Reservoir use off-chain orderbooks (can't call from DelegatableNotes on-chain)
- SudoSwap is an AMM, not an orderbook — different mental model, would need architecture rethink
- Our contract is simple/generic enough that publishing it isn't a legal concern
- The real legal concerns are about the UI/descriptions, not the contracts

## SDK unit test setup (Jan 2026)

Added unit test infrastructure and tests for sdk/src/displayable-document.ts:
- Set up Mocha + tsx in SDK workspace (matching integration-tests pattern)
- 63 unit tests covering: toCanonicalJson, validateDisplayableDocument, createDisplayableDocument, isDisplayableDocument, createStatement, document identity via canonical JSON
- Added `npm run sdk:test` to root package.json (runs before hardhat:test in `npm run test`)
- SDK tsconfig excludes test files from build output
- Files changed: sdk/package.json, sdk/tsconfig.json, sdk/.mocharc.json, sdk/src/displayable-document.test.ts, package.json

Next: TODO.md says to work on displayable-documents decoupling. The SDK types/validation/creation are tested now. Possible next steps: displayable document renderer (for UI), or broader decoupling work from specs/decoupling.md.

High-level stuff is all in order, it's time to implement. Next AI instance should use task-chooser to pick a task from TODO.md.
