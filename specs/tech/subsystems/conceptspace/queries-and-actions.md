# Queries and actions

Conceptspace clients query statements, beliefs, implication attestations, nudger publications, and trust settings through the SDK/indexer layer.

Actions include creating statements, signing beliefs, configuring trusted attesters/nudgers, and following suggestions to related statements. On-chain actions should remain explicit user intent; derived support and suggestions are computed views.
