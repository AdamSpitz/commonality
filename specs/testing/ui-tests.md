# UI testing

The UI is written using Vite and React.

We just added Vitest + Testing Library, but haven't written any UI tests yet.

Would it be useful to have end-to-end UI tests too?

In general, I would like it if AI could do a lot of the UI-testing work, rather than me needing to open up the browser myself and click buttons. I don't really know the extent to which this is possible.
