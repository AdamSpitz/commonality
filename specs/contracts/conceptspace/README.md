# Conceptspace smart contracts

I had an AI generate these from our specs. It generated a whole Hardhat project, but here I'm just copying in the actual contracts and the unit tests; the rest of the Hardhat project is probably easy to regenerate and nothing else in the system really depends on it as an integration point, but having the contracts here in the specs can be useful so that the rest of the system knows what the interface will be.
