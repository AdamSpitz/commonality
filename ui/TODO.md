## UI to-dos

## Concept Space UI - MVP

Implementation is mostly done and well-tested.

**Unit tests: COMPLETE.** All components and pages have comprehensive unit tests (265 tests total).

**E2E tests: STARTED.** Playwright installed and configured with initial "tracer bullet" tests (2 tests).

See [test-plan.md](test-plan.md) for:
- Detailed test strategy (unit tests + E2E tests)
- Component-by-component test coverage status
- How to run tests

## E2E Testing with Docker Backend - Implementation Plan

### Current State
- Playwright installed with 2 basic "tracer bullet" tests
- Tests verify UI renders without backend (smoke tests only)
- Docker setup exists for integration tests (hardhat-node, IPFS, indexer/GraphQL)

### Goal
Integrate Docker backend into E2E tests to enable full-stack workflow testing with real contracts, IPFS, and GraphQL indexer.

### Implementation Steps

#### 1. Docker Integration
- [x] Create `ui/e2e/global-setup.ts` to start Docker Compose services
  - Run `docker-compose up -d --build` (reuse existing docker-compose.yml)
  - Wait for healthchecks to pass (hardhat-node, ipfs, indexer)
  - Services expose: blockchain (8545), IPFS (5001/8080), GraphQL (42069)
  - **NEW:** Copy contract addresses from integration-tests/.env.local to ui/.env
    - Makes VITE_BELIEFS_CONTRACT_ADDRESS and VITE_MUTABLE_REF_UPDATER_CONTRACT_ADDRESS available
- [x] Create `ui/e2e/global-teardown.ts` to clean up
  - Run `docker-compose down -v` to remove containers and volumes
  - Ensures fresh state for next test run
- [x] Update `playwright.config.ts` to use global setup/teardown
- [x] Create `scripts/run-e2e-tests.sh` script (similar to run-integration-tests.sh)

#### 2. Wallet Connection Testing
- [x] Configure Playwright to inject Hardhat test accounts as wallets
  - Used wagmi's mock connector (more reliable than browser extensions)
  - Hardhat provides 10 test accounts (ACCOUNT_0 through ACCOUNT_9)
  - Each account has 10,000 ETH and known private keys
- [x] Reuse SDK's `TEST_PRIVATE_KEYS` from `@commonality/sdk`
- [x] Create test helpers to connect specific Hardhat accounts
  - Created `e2e/fixtures/wallet.ts` with WalletFixture class
  - Provides `wallet.connect('ACCOUNT_0')` method for tests
- [ ] Assign different accounts to different test files (avoid nonce conflicts)
  - Can reuse pattern from integration-tests/src/utils/test-utils.ts
  - Not needed yet since tests are isolated, but will be needed for complex workflows

#### 3. Test Coverage - Critical Workflows
- [x] **Wallet connection flow** (tracer bullet done)
  - Connect Hardhat account via ConnectKit Mock Connector
  - Verify connected address displays correctly
  - 3 passing tests in wallet-connection.spec.ts
- [ ] **Statement creation workflow** (infrastructure ready, needs debugging)
  - Contract addresses now copied to ui/.env automatically
  - Next step: Debug why form submission times out
  - Recommended: Start with simpler test (just fill form, don't submit)
  - Then add console logging and screenshots to diagnose issue
  - See progress.txt for detailed breakdown suggestions
- [ ] **Belief expression workflow**
  - Navigate to existing statement
  - Express belief (sign transaction)
  - Wait for indexing
  - Verify belief count updates
  - Verify believer appears in supporters list
- [ ] **User profile workflow**
  - View profile for connected account
  - Verify statements and beliefs display correctly
  - Test pagination if applicable

#### 4. Test Infrastructure
- [ ] Create E2E test utilities (ui/e2e/utils/)
  - GraphQL polling helpers (wait for indexer sync)
  - Wallet connection helpers
  - Transaction waiting utilities
  - Contract address loading from .env
- [ ] Add environment variable loading for E2E tests
  - Contract addresses from .env (generated by hardhat-deploy)
  - RPC_URL, GRAPHQL_URL, IPFS endpoints
- [ ] Configure proper test timeouts
  - Account for indexer sync delays
  - Integration tests show ~50-100ms polling works well

#### 5. Documentation
- [ ] Update test-plan.md with E2E backend integration details
- [ ] Document how to run E2E tests (npm script)
- [ ] Add troubleshooting guide for Docker issues
- [ ] Document Hardhat account management for tests

### Benefits
- Test real smart contract interactions, not mocks
- Validate full stack: UI → wagmi → contracts → indexer → GraphQL → UI
- Reproducible test environment (fresh Docker state each run)
- Same infrastructure as integration tests (consistency)
- Test accounts with pre-funded ETH (no faucet needed)

## Other subsystem UIs

Not even started yet, I think.