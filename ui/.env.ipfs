VITE_ROUTER_MODE=hash
# Do not set VITE_EVENT_CACHE_URL here. IPFS/deployed builds must get their
# event-cache URL from ui/.env, root .env-derived publish env, or explicit
# operator environment so testnet/mainnet bundles cannot accidentally bake in
# the local http://localhost:42069 indexer default.
