# To Do

This is the project's inbox; use this for tasks that might be suitable for an LLM to do.

If you have stuff that needs human attention, you can put it in [Adam's inbox](/inbox.md) instead. See [task autonomy tiers](/workflow/task-tiers.md).

----

- [x] **(Tell)** Implement the proof-of-progress **updates-channel link** (decision settled 2026-06-18; see [specs/product/proof-of-progress.md](specs/product/proof-of-progress.md)). Add a first-class, structured `updatesUrl` (or small `links` list) to the project IPFS metadata document and surface it prominently in project creation (`ui/src/lazy-giving/pages/CreateProjectPage.tsx`) and on the project page, presented as a plain link to a channel the creator already runs (X, Substack, blog, YouTube, GitHub, Discord). This is the whole feature — do NOT build or embed any hosted/open comment system (rejected: spam/abuse + moderation liability we'd own). Read-only inline embedding of the linked feed is explicitly deferred.

- [ ] **(Tell)** Build the contribution sequencing UI/service for the no-custody on-ramp path: start contribution, create on-ramp session, detect USDC arrival, handle allowance if needed, send `buyERC1155` from the user's wallet, show confirmation/retry/error states, and connect the result to leaderboard/status display. See [specs/tech/bridges.md](specs/tech/bridges.md).

- [ ] **(Tell)** Run the Privy + Pimlico embedded-wallet spike (provider is ratified — Privy, 2026-06-18 — this is feasibility, not a provider re-eval). Confirm the `[confirm in spike]` items in [specs/tech/bridges.md](specs/tech/bridges.md): (1) **load-bearing** — the chosen on-ramp will deliver USDC to a *counterfactual* (undeployed) EIP-4337 account address; if not, fall back to eagerly deploying the account via a tiny sponsored UserOp right after login; (2) Pimlico+Privy follow the `initCode`-on-first-UserOp deploy pattern and the paymaster sponsors that deploy-inclusive first op; (3) Privy key export works against a real account (pre-mainnet gate); (4) login/recovery modal UX holds the walletless framing end-to-end; and sanity-check per-wallet/MAU economics before mainnet. See also [specs/tech/sponsored-gas.md](specs/tech/sponsored-gas.md) §1.

- [ ] **(Tell)** Finish sponsored-gas support. Initial `CreatorGasTank` contract spike is done (per-creator ETH tanks, anyone-can-fund, self-enrollment, configurable per-wallet caps, SimpleAccount-shaped validation, `postOp` debit, unit tests). Remaining: confirm Privy+Pimlico account ABI and finalize decoder; tune production caps from real UserOp overhead; deploy to testnet and add bundler/UI wiring plus behavioral verifier monitoring; implement `GasTankFunder` USDC→ETH swap adapter. See [specs/tech/sponsored-gas.md](specs/tech/sponsored-gas.md).

- [ ] **(Tell)** Build failed-project refund UX for embedded-wallet contributors: detect refundable positions, call `refundERC1155`, sponsor gas where appropriate, show refunded USDC in the user's wallet, and offer clear next steps (keep USDC, re-contribute, or use a licensed offramp/KYC flow). See [specs/tech/bridges.md](specs/tech/bridges.md).

- [ ] **(Tell)** Add contribution notifications for walletless/on-ramp users: confirmation email, transaction link, refund-available notice, and clear copy explaining that card contributions become onchain USDC/token transactions rather than Commonality-held funds. See [specs/tech/bridges.md](specs/tech/bridges.md).

- [ ] **(Tell)** Finish/polish "successful projects" on cause boards (see [specs/product/successful-projects.md](specs/product/successful-projects.md)). Remaining work: run/verify the end-to-end UI path with indexed data; consider replacing the crude trust/confidence score (attester count) with the intended trust-weighted score/discovery-slider behavior. Success-attestation branch UI tests for `AlignmentAttestationsSection` are done. Policy questions on open posting, reputation/decay, and domain-scoped success trust are decided in the spec. The card CTA now links directly to the LazyGiving secondary-market section instead of the old inert `?retro=1` placeholder.

- [x] **(Tell)** Contract-versioning prep, indexer/SDK side (see [specs/tech/contract-versioning.md](specs/tech/contract-versioning.md)): audited SDK folds/UI/service paths for bare onchain auto-increment IDs and re-keyed the wired Class 2/3 surfaces by `(contractAddress, id)` where needed. Secondary market, DelegatableNotes, RecurringPledges, ContentRegistry, LazyGiving contribution chains, note-intent paths, note detail routes, My Notes actions, recurring-pledge scheduler execution, note-funded purchases, and secondary-market fulfill actions are now scoped. SDK event fetch helpers for these version-sensitive surfaces now fetch merged event streams by event name across indexed contract versions instead of only the current singleton address. Closure audit recorded in [workflow/contract-versioning-closure-audit-2026-06-22.md](workflow/contract-versioning-closure-audit-2026-06-22.md).

- [x] **(Tell)** Class-1 log contract v2 cleanup: before deploying a v2 of `Beliefs`, `Implications`, `AlignmentAttestations`, `TrustRegistry`, or `MutableRefUpdater`, update their SDK query helpers to merge same-name events across indexed versions instead of filtering only the current configured contract address. This is separate from the completed auto-increment-ID collision work because these append-only/opinion logs do not have per-contract restarted numeric IDs. SDK query helpers now fetch `DirectSupport`, `ImplicationAttestation`, `AlignmentAttestation`/`SuccessAttestation`, `TrustSet`, and `RefUpdated` by event name/topic across indexed versions rather than by singleton configured address.

- [x] **(Tell)** Contract-versioning prep, indexer config: replace one-env-var-per-contract with a per-chain deployment manifest supporting *lists* of `{address, startBlock}` per logical contract, so adding a v2 contract is a config change. `deployments/*.env` is halfway to being that manifest. Indexer now accepts `INDEXER_DEPLOYMENT_MANIFEST` JSON with per-chain logical contract deployment lists while preserving legacy env vars for backward-compatible local/testnet config.

- [x] **(Tell)** Publish a deployment-manifest pointer onchain (MutableRefUpdater ref or ENS text record) so clients can discover current contract versions, per [specs/tech/contract-versioning.md](specs/tech/contract-versioning.md). Added the publishable manifest generator (`scripts/deployment-manifest.mjs`), generated `deployments/base-sepolia.manifest.json`, documented the discovery flow in `indexer/README.md`, and added `hardhat/scripts/publish-deployment-manifest-ref.js` to publish the pinned manifest URI under the `commonality.deployment-manifest` MutableRefUpdater ref. The actual testnet transaction still requires operator secrets plus a pinned manifest CID.

- Remaining recurring-pledges work is operational: deploy the updated contracts to testnet, regenerate `deployments/base-sepolia.env`/`render.yaml`, copy/fund the scheduler key, set `RECURRING_PLEDGE_SCHEDULER_ENABLED=true`, redeploy workers, and verify a due pledge produces a `StandingPledgeExecuted` event through the indexer.

- [ ] Hardhat 2→3 migration — defer until after current testnet stabilization, but revisit before mainnet. Treat as a standalone migration project, not a dependency bump.

- [ ] Verify the Render/Ponder deploy fix over a few normal indexer redeploys: `commonality-indexer` now has a tiny persistent disk so Render should do stop-before-start deploys instead of rolling deploys, avoiding Ponder `DATABASE_SCHEMA` lock conflicts. If lock failures recur, split the indexer into a singleton writer/worker plus a separately deployed read-only web/API service. See [workflow/deployment.md](workflow/deployment.md#known-render-indexer-deployment-trap-ponder-schema-lock).

- [x] Do another smart-contract audit pass. Targeted 2026-06-22 follow-up recorded in [workflow/reviews/smart-contract-audit-2026-06-22.md](workflow/reviews/smart-contract-audit-2026-06-22.md): Slither remained clean at High/Medium severity, and the open content-funding threshold/deadline findings were fixed in `CreatorAssuranceContractFactory` with Hardhat coverage. One prior low-severity direct-call ChannelEscrow footgun remains documented but unchanged.

- [x] **(Tell)** Make the LazyGiving recipient field foolproof — replace the raw `0x…` text box in `ui/src/lazy-giving/pages/CreateProjectPage.tsx` with a layered picker (see [specs/product/foolproof-project-creation.md](specs/product/foolproof-project-creation.md)): (1) explicit "Send to me / my account" default; (2) pick from a saved contact list of previously-vetted recipients, persisted client-side and appended to whenever a new recipient is confirmed; (3) ENS name resolution with a plain-language "this resolves to X — is that right?" confirmation, plus an optional tiny test transaction before committing. Adam explicitly asked for the contact list and ENS support. (Do NOT build the embedded-wallet claim-later path here — it's the "Later" item in [specs/product/foolproof-project-creation.md](specs/product/foolproof-project-creation.md) #4, built as a thin layer on the ratified Privy integration.)

- [ ] **(Tell)** Build the **donation-first reframe** of LazyGiving create + donate (approved 2026-06-18; see the "Adjacent issues" section of [specs/product/foolproof-project-creation.md](specs/product/foolproof-project-creation.md)). Large-task implementation plan is in [workflow/donation-first-reframe-plan-2026-06-22.md](workflow/donation-first-reframe-plan-2026-06-22.md). Keep the general ERC-1155 token mechanism — no separate "simple mode" — and steer with defaults/guidance. Progress: create-page steps 1–3 are done: token capacity math is extracted/tested; defaults frame the form as dollar goal + cap choice + visible giving options with a default `$1 Donation` option and hidden token IDs; suggested giving levels, donor-eye preview, literal "what gets created" preview, exact stop-at-goal capacity math, and removed-small-denomination warning are implemented. Donor-side plan step 4 is done as a pure/tested allocation helper (`ui/src/lazy-giving/purchaseAllocation.ts`) covering exact `$1` allocation, snapped fixed-tier fallback, add-on+remainder allocation, and unavailable/sold-out options. Donor-side plan step 5 is done: `BuyTokensSection` now leads with a donor-first give-amount UI, optional reward add-ons, allocation-helper-backed `buyERC1155` arrays, refund/permanence/network-fee copy, and tests for exact donation, mixed reward add-on purchase, and impossible amount fallback. Suggested next one-shot subtask: plan step 6, copy sweep/compatibility pass for remaining LazyGiving primary-market token-first wording while preserving genuine secondary-market language. Do NOT build the embedded-wallet claim-later recipient path (the "Later" item in [specs/product/foolproof-project-creation.md](specs/product/foolproof-project-creation.md) #4 — a thin layer on the ratified Privy integration).

- [x] **(Tell)** Add a **client-side PII warning** to the statement creator. Before a draft is pinned/committed, scan it locally for likely personal info (phone numbers, emails, SSNs, street addresses, and — optionally via a local LLM pass — apparent full names of private individuals) and show an **advisory, non-blocking** warning: "This looks like it contains a phone number. Statements are public and permanent — post anyway?" Rationale: statement content is on IPFS with immutable onchain references (`ContentRegistry` + never-overwritten mutable-ref history), so once published it can't be truly erased — prevention at input time is the only reliable lever. Constraints: keep the scan client-side so the draft isn't shipped anywhere just to be checked; warn-only, never block (avoid false-positive lockout/workarounds); regex for structured patterns, optional LLM for contextual cases. Out of scope (deferred): any unpin/"delete statement"/takedown machinery — it can only *stop serving from our UI*, not erase, so don't build it as if it were erasure.

- [x] **(Tell)** Shrink the contract owner-lever surface — two safe, behavior-preserving changes that remove standing admin levers (groundwork for the governance/timelock story; see [inbox.md](/inbox.md)). (1) **`DelegatableNotes.setRecurringPledgeRegistry` → set-once**: only callable while `recurringPledgeRegistry == address(0)`, then permanently frozen. It's set exactly once at deploy (`hardhat/scripts/deploy.js:233`, `deploy-incremental.js:163`) and never again; a constructor arg is impossible because Notes↔RecurringPledges is a true mutual/`immutable` dependency. Closing it stops an owner from later re-pointing the registry (the privileged caller gated at `DelegatableNotes.sol:343`). (2) **`ChannelRegistry.setVetoWindowDuration` → monotonic-lengthen only**: reject any value shorter than the current `vetoWindowDuration` (default `7 days`, line 113). The setter is never called anywhere today; shortening is the only harmful direction (it can retroactively expire in-flight vetoes, since the window is measured from each channel's `_controlTakenAt`), and lengthening is strictly more protective. Update/extend unit tests for both. NOTE: `setVerifier` and the factory-authorization levers are deliberately NOT in scope here — they're the harder governance fork still under discussion.

- [x] Build LLM-based per-page verifier checks that loop the derived page inventory, one per analysis kind: "is the page usable?", "does it look visually appealing?", "does it work well on mobile?", etc. The sampled/manual checks `review.page-copy-sense`, `review.page-usability`, `review.page-visual-appeal`, and `review.page-mobile-usability` are all wired into `product.messaging`. The deterministic `review.page-links` check is the worked template. Cost guardrails are manual-triggered and/or sampled. See [verifier/checks/review/](verifier/checks/review/) for the four LLM leaves.
