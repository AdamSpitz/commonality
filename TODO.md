# To Do

This is the project's inbox; use this for tasks that might be suitable for an LLM to do.

If you have stuff that needs human attention, you can put it in [Adam's inbox](/inbox.md) instead. See [task autonomy tiers](/workflow/task-tiers.md).

----

- [ ] **(Tell)** Build the contribution sequencing UI/service for the no-custody on-ramp path: start contribution, create on-ramp session, detect USDC arrival, handle allowance if needed, send `buyERC1155` from the user's wallet, show confirmation/retry/error states, and connect the result to leaderboard/status display. See [specs/tech/bridges.md](specs/tech/bridges.md).

- [ ] **(Tell)** Run the Privy + Pimlico embedded-wallet spike (provider is ratified — Privy, 2026-06-18 — this is feasibility, not a provider re-eval). Confirm the `[confirm in spike]` items in [specs/tech/bridges.md](specs/tech/bridges.md): (1) **load-bearing** — the chosen on-ramp will deliver USDC to a *counterfactual* (undeployed) EIP-4337 account address; if not, fall back to eagerly deploying the account via a tiny sponsored UserOp right after login; (2) Pimlico+Privy follow the `initCode`-on-first-UserOp deploy pattern and the paymaster sponsors that deploy-inclusive first op; (3) Privy key export works against a real account (pre-mainnet gate); (4) login/recovery modal UX holds the walletless framing end-to-end; and sanity-check per-wallet/MAU economics before mainnet. See also [specs/tech/sponsored-gas.md](specs/tech/sponsored-gas.md) §1.

- [ ] **(Tell)** Finish sponsored-gas support. Initial `CreatorGasTank` contract spike is done (per-creator ETH tanks, anyone-can-fund, self-enrollment, configurable per-wallet caps, SimpleAccount-shaped validation, `postOp` debit, unit tests). Remaining: confirm Privy+Pimlico account ABI and finalize decoder; tune production caps from real UserOp overhead; deploy to testnet and add bundler/UI wiring plus behavioral verifier monitoring; implement `GasTankFunder` USDC→ETH swap adapter. See [specs/tech/sponsored-gas.md](specs/tech/sponsored-gas.md).

- [ ] **(Tell)** Build failed-project refund UX for embedded-wallet contributors: detect refundable positions, call `refundERC1155`, sponsor gas where appropriate, show refunded USDC in the user's wallet, and offer clear next steps (keep USDC, re-contribute, or use a licensed offramp/KYC flow). See [specs/tech/bridges.md](specs/tech/bridges.md).

- [ ] **(Tell)** Add contribution notifications for walletless/on-ramp users: confirmation email, transaction link, refund-available notice, and clear copy explaining that card contributions become onchain USDC/token transactions rather than Commonality-held funds. See [specs/tech/bridges.md](specs/tech/bridges.md).

- [x] **(Tell)** Finish/polish "successful projects" on cause boards (see [specs/product/successful-projects.md](specs/product/successful-projects.md)): replace the first-pass direct-vs-indirect success confidence score with richer trust-graph weighting/discovery-slider behavior. Trust-graph **weighting** is done (success confidence now scales each vouch by the viewer's transitive trust score for the attester, plumbed from the Subjectiv worker through `useTrustedSet` → cause board → `SuccessfulProjectsList`; falls back to the flat 2:1 count when no trust network is available). The **discovery-slider UI control** ("my network → +1 hop → anyone") is now live on the Successful tab, surfacing the existing `maxHops` trust-traversal knob via a dedicated `useTrustedSet` call scoped to the tab (`SuccessfulProjectsTab`), with `maxHops` plumbed end-to-end (computation → worker → cache key bumped `v2`→`v3` → hook) and "anyone" dropping the trust filter entirely. Follow-up: mirror the same control onto the Aligned tab once that view is ready for it. Policy questions on open posting, reputation/decay, and domain-scoped success trust are decided in the spec.

- Remaining recurring-pledges work is operational: deploy the updated contracts to testnet, regenerate `deployments/base-sepolia.env`/`render.yaml`, copy/fund the scheduler key, set `RECURRING_PLEDGE_SCHEDULER_ENABLED=true`, redeploy workers, and verify a due pledge produces a `StandingPledgeExecuted` event through the indexer.

- [ ] **(Tell)** Wire the SDK `identity` primitives (`computeAnonymizedId`, `ProofTier`, `foldAnonymizedBelieverIds`/`unionAnonymizedBelieverIds` in `sdk/src/subsystems/identity/`) into the actual Tally count path and render tiered head-counts in the UI. **Foundation landed:** the anonymized-ID derivation (`hash(anchor_address, app_salt)`, public salt, case-normalized), the proof-strength **tier/value** model (`ProofTier.NONE/ASSERTED/ONE_ATTESTATION/MULTIPLE_ATTESTATIONS` — not a boolean, so no later schema migration), and the set-union/dedupe-key helpers over DirectSupport events are in the SDK with tests. **Dedupe seam wired (2026-06-22):** `getIndirectSupporters` now dedupes indirect supporters by anonymized anchor ID (via `foldAnonymizedBelieverIds` + `unionAnonymizedBelieverIds`) instead of raw address, so an account that signed several equivalent (mutually-implying) statements counts once; today address→ID is 1:1 so counts are unchanged, and the disbeliever-exclusion + first-implication-wins via-statement behavior is preserved. **Tiered head-count plumbed + rendering (2026-06-22):** `computeTieredHeadCount` groups a deduped supporter-ID set into cumulative proof-strength thresholds (`total` / `assertedOrHigher` / `oneAttestationOrHigher` / `multipleAttestationsOrHigher`); `getStatementSupportTieredHeadCount` computes it over the full deduped supporter base (direct believers ∪ indirect supporters, target-disbelievers excluded); `getStatementWithContent` metrics now carry `tieredSupporters`, and `SupportMetrics` renders the "— M with ≥N attestations" breakdown. The breakdown line only appears when attestation-backed supporters exist, so before any provider is wired up the UI stays quiet and the headline never reads as a verified-human count (per `unique-human-id.md` caveat #1). Remaining: persist a `Map<anonymizedId, ProofTier>` once a proof provider exists (the `knownTiers` seam is plumbed end-to-end and defaults to tier 0 today), feed it from the tier-0→1 self-declaration (see the next item) and eventually Gitcoin Passport. See [specs/tech/shared/unique-human-id.md](specs/tech/shared/unique-human-id.md).

- [x] **(Tell)** Ship proof-of-personhood tiers 0–1: a per-account on-chain self-declaration ("this is my one Commonality account") that moves an account from tier 0 → 1, plus Tally counts that **group by tier** and a UI that renders the tiered head-count string. **Landed (2026-06-22):** new `AccountAssertions.sol` contract (`assertSingleAccount` / `revokeAssertion`, `AccountAssertionSet` event, last-write-wins, no admin) in `hardhat/contracts/subjectiv/`, with Hardhat tests. Indexed via Ponder (`AccountAssertions` registered in `ponder.config.ts` + `events-cache/index.ts`) and decoded in the SDK (`decodeAccountAssertionSetEvent`). New identity subsystem queries/actions: `getKnownProofTiers` builds the `Map<anonymizedId, ProofTier>` from `AccountAssertionSet` events (asserted → tier 1, revoked → absent), `getAccountAssertion` reads one account's status, and `assertSingleAccount`/`revokeAssertion` write actions exist. `getStatementWithContent` now auto-populates `knownTiers` from the event cache when the caller doesn't supply them, so the tiered head-count lights up automatically. `SupportMetrics` renders the tier-1 line ("— N claimed this is their one account") with a self-claim caveat, separate from the attestation-backed line. A `SingleAccountAssertionSection` on the Settings page lets a user make/revoke the assertion. Deploy script wires `AccountAssertions` + `VITE_ACCOUNT_ASSERTIONS_CONTRACT_ADDRESS`. Per `unique-human-id.md` caveat #1, copy reads "assert"/"claim," never "we checked." **Remaining (operational):** deploy `AccountAssertions` to testnet, regenerate `deployments/base-sepolia.env`/`render.yaml`/`ui/.env`, and verify the tier-1 line appears on a statement after asserting. See [specs/tech/shared/unique-human-id.md](specs/tech/shared/unique-human-id.md).

- [ ] **(Tell)** Continue defining per-module public APIs so UI feature modules can eventually be published as packages / split into their own repos (this is the prerequisite for the [separate-repo end state](/docs/founder/standing-up-a-vertical.md#the-end-state-a-vertical-is-its-own-repo); the dependency *layering* is already clean — the gap is that the whole UI is one npm package and modules are bounded by folders, not declared interfaces). **Pattern (landed for `delegation`, commit 95f8c80f):** (1) add a root `ui/src/<module>/index.ts` barrel re-exporting *only* the intended eager public surface (components/utils/hooks); (2) route all external consumers through the barrel (`from '../../<module>'`), never deep paths; (3) keep lazy route targets (pages loaded via dynamic `import()` in domain manifests) as deep dynamic imports — they're the subpath half of the public API and map to `@commonality/<module>/pages/*`, so don't fold them into the eager barrel or you collapse code-split chunks; (4) add a `no-restricted-imports` boundary block in `ui/eslint.config.js` (copy the `delegation` block; barrel + lazy `pages/*`/`LandingPage` allowed, everything else forbidden) — the lint rule reliably finds deep importers the grep misses, and surfaces mis-couplings (e.g. consumers importing `shared` utils *through* a module). **Do this in-monorepo first** (cheap, reversible, enforceable); only convert to *published* packages + extract repos once the substrate API stabilizes (trigger is API stability, not a date). **Next modules, in order of value/effort:** `content-funding` and `lazy-giving` — both harder than `delegation` because verticals reach into their `pages/...` and they have the widest consumer surface, so the real work is *deciding* public-vs-internal, not just rerouting. Then `fundingportals`, `conceptspace`, and `shared`. **`content-funding` landed (2026-06-22):** root `ui/src/content-funding/index.ts` barrel re-exporting the eager public surface (`ContentAttestationSummary`, `ContentSubmissionForm`, `ContentFundingProjectSection`, `useClaimFlow`, `useContentFundingState` + `ContentAttestationInfo`, `getChannelDisplayLabels` + `ChannelDisplayLabels`/`ChannelDisplayMetadata`); all external eager consumers rerouted through it; `pages/*` kept as the deep subpath half (domain route wrappers in `domains/content-funding/` and `domains/civility/` still load them directly, preserving code-split chunks). Boundary enforced via a `no-restricted-imports` block in `ui/eslint.config.js` — but it uses a `regex` matcher, **not** the glob `group` form the `delegation` block uses, because the glob form has two bugs that bite content-funding: (1) the `ignore`-backed matcher can't re-include children of an excluded `pages/` directory (gitignore parent-dir rule), so `!**/content-funding/pages/*` silently fails to un-restrict page imports; and (2) `**/content-funding/*` also matches the unrelated `src/domains/content-funding/` directory. The regex `(?:\.\./)+content-funding/(?!pages(?:/|$))` requires at least one `../` before `content-funding/` (every real feature-module consumer reaches `src/content-funding` via `../`-relative paths; the domains wrapper is reached via `./content-funding/...`), allows the barrel (no trailing path) and `pages/*`, and forbids everything else deep. `delegation` dodged both bugs only because its `domains/delegation/` dir has a single `LandingPage` (explicitly negated) and no `domains/index.ts` import of `./delegation/manifest`; when `delegation` is touched next, consider switching its block to the same regex form for consistency/correctness. External test files keep their `vi.mock('...content-funding/hooks|components/...')` deep-path mocks (lint doesn't check `vi.mock` strings) — they still intercept because the barrel live-re-exports from those same underlying modules. Feedback loop green: `npm run typecheck --workspace=ui`, `npm run lint --workspace=ui` (0 errors), and vitest for all touched modules (content-funding + conceptspace + fundingportals + the domain wrappers). **`lazy-giving` landed (2026-06-22):** root `ui/src/lazy-giving/index.ts` barrel re-exporting the eager public surface — the project-status helpers `getProjectStatus`/`STATUS_COLORS`/`STATUS_LABELS`/`formatRelativeDeadline` (+ `ProjectStatus` type) from `utils`, which is what `fundingportals` legitimately needs to render aligned-project cards on cause boards. The component surface (`BuyTokensSection`, `ProjectHeader`, `Leaderboard`, …) is intentionally **not** re-exported: those are only consumed by lazy-giving's own pages/landing today, so they stay module-internal until an external consumer actually needs one (the public-vs-internal decision this module required). Rerouted every external eager consumer through the barrel — `fundingportals/components/AlignedProjectCard.tsx`, `AlignedProjectsList.tsx`, and the real `getProjectStatus` import in `AlignedProjectCard.test.tsx` (the test's `vi.mock('../../lazy-giving/utils', …)` / `vi.importActual` deep-path strings were left as-is; lint doesn't check those, and they still intercept because the barrel live-re-exports from `./utils`). `pages/*` stays as the deep subpath half of the public API — `domains/lazy-giving/manifest.tsx` and the `domains/content-funding`/`domains/civility` ContentPages wrappers still `import('../../lazy-giving/pages/…')` directly, so code-split chunks are preserved. Boundary enforced via a new `no-restricted-imports` block in `ui/eslint.config.js` using the **same regex form as `content-funding`** (`(?:\.\./)+lazy-giving/(?!pages(?:/|$))`) — not the `delegation` glob form, for the same two reasons (the `ignore`-backed glob matcher can't re-include children of an excluded `pages/` dir, and `**/lazy-giving/*` would also match the unrelated `src/domains/lazy-giving/` directory). `src/lazy-giving/**` is excluded via `ignores` so the module's own files can still import their own internals. Feedback loop green: `npm run typecheck --workspace=ui`, `npm run lint --workspace=ui` (0 errors, 1 pre-existing unrelated warning), and vitest for all touched modules — lazy-giving + fundingportals + the domain wrappers (34 files, 603 tests). **`fundingportals` landed (2026-06-22):** root `ui/src/fundingportals/index.ts` barrel re-exporting the eager public surface — just the two components that actually cross the module boundary today: `FundingPortalSummary` (rendered on the conceptspace `StatementPage`) and `AlignmentAttestationsSection` (rendered on the lazy-giving `ProjectDetailPage`). The rest of the component surface (`AlignedProjectCard`, `AlignedProjectsList`, `AttestAlignmentForm`, `DelegatableNotesSection`, `SuccessfulProjectsList`/`Tab`, `DiscoverySlider`, …) and all utils are consumed only by fundingportals' own pages/components, so they stay module-internal until a real external caller appears (promote-on-demand, same as lazy-giving's component surface). Rerouted both external eager consumers through the barrel — `conceptspace/pages/StatementPage.tsx` and `lazy-giving/pages/ProjectDetailPage.tsx`. `pages/*` stays as the deep subpath half of the public API — `domains/alignment/manifest.tsx` and `domains/tally/manifest.tsx` still `import('../../fundingportals/pages/…')` directly via dynamic `import()`, so code-split chunks are preserved. Boundary enforced via a new `no-restricted-imports` block in `ui/eslint.config.js` using the **same regex form** (`(?:\.\./)+fundingportals/(?!pages(?:/|$))`); `src/fundingportals/**` excluded via `ignores`. (No `domains/fundingportals/` directory exists, so the glob form's false-match-on-domains-dir bug #2 wouldn't have bitten here, but the pages-negation bug #1 still would have — the regex form is kept for consistency across all four module blocks.) No external `vi.mock` into fundingportals existed, so no test deep-path strings to preserve. Feedback loop green: `npm run typecheck --workspace=ui`, `npm run lint --workspace=ui` (0 errors, 1 pre-existing unrelated warning), and vitest for all touched modules — fundingportals + conceptspace + lazy-giving + the alignment/tally domain wrappers (43 files, 903 tests). **Next up: `conceptspace`, then `shared`** (per the value/effort order above). Per-module feedback loop: `npm run typecheck --workspace=ui`, `npm run lint --workspace=ui`, and `vitest run` for touched modules; all must stay green (behavior-preserving refactor).

- [ ] Hardhat 2→3 migration — defer until after current testnet stabilization, but revisit before mainnet. Treat as a standalone migration project, not a dependency bump.

- [ ] Verify the Render/Ponder deploy fix over a few normal indexer redeploys: `commonality-indexer` now has a tiny persistent disk so Render should do stop-before-start deploys instead of rolling deploys, avoiding Ponder `DATABASE_SCHEMA` lock conflicts. If lock failures recur, split the indexer into a singleton writer/worker plus a separately deployed read-only web/API service. See [workflow/deployment.md](workflow/deployment.md#known-render-indexer-deployment-trap-ponder-schema-lock).
