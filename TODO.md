# To Do

This is the project's inbox; use this for tasks that might be suitable for an LLM to do.

If you have stuff that needs human attention, you can put it in [Adam's inbox](/inbox.md) instead. See [task autonomy tiers](/workflow/task-tiers.md).

When an item from this page is done and no longer needs an LLM implementor's attention, don't mark it "done", just delete it. I don't want this file to get cluttered with already-completed items.

----

- Finish the operational cutover of the Alignment site to `aligning.works`. The visible site name is Aligning, Base Sepolia deployment config and cross-domain routing use `https://aligning.works`, platform API CORS includes that origin, and the refreshed testnet UI was published on 2026-07-24 to the Alignment IPNS name (`QmZyfDXPAGbyhYxRGTG2uS6siFjSdkxfSQPYHQo2PnoJAs`, IPNS sequence 11). Remaining: update the domain at its current Network Solutions DNS (`ns1/ns2.worldnic.com`) from parked address `74.91.138.137` to the IPNS-backed gateway, and provision a certificate for `aligning.works`. The parked endpoint currently serves Network Solutions' “under construction” page with a `*.hostingplatform.com` certificate, so canonical-domain and cross-domain-link smoke tests remain blocked. Internal `alignment` identifiers can remain.

- [ ] **(Tell)** Finish sponsored-gas support. Code foundation is in place (`CreatorGasTank`, Kernel/SimpleAccount decoding, verifier smoke, `GasTankFunder`) and the UI now has an ERC-7677 paymaster endpoint path (`/sponsored-gas/paymaster`) for Privy/Pimlico to attach the custom onchain paymaster to Kernel v3 UserOps. The platform API paymaster endpoint is now deployed and responding at the configured Base Sepolia URL: a 2026-07-22 live probe confirmed ERC-7677 JSON-RPC handling and `pm_getPaymasterStubData` request validation, and `testnet.sponsored-gas` now continuously guards that the endpoint returns the configured paymaster and correctly inferred project. Remaining: test it in the real Privy flow, confirm against a live Privy+Pimlico UserOp trace, and tune production caps from real overhead. `GasTankFunder` deployment is blocked on choosing usable testnet swap infrastructure: a 2026-07-24 Base Sepolia probe confirmed canonical WETH exists, but the canonical Base Uniswap v3 router/factory addresses have no code on chain 84532, and the deployed USDZZZ payment token therefore has no canonical Uniswap v3 test pool to exercise. (Kernel-v3/ERC-7579 decoder bug found + fixed + confirmed against real permissionless output on 2026-07-14 — see [workflow/sponsored-gas-live-trace.md](workflow/sponsored-gas-live-trace.md); an on-chain browser trace remains the final end-to-end check.) See [specs/tech/sponsored-gas.md](specs/tech/sponsored-gas.md) for implementation status/details.

- [ ] **(Tell)** Live-verify embedded-wallet failed-project refunds against a Privy embedded wallet on testnet once the Privy/Pimlico paymaster endpoint and an enrolled funded gas tank are available. Code wiring is in place: refund UX now runs the required ERC-1155 project approval before `refundERC1155`, and both calls match the sponsored-gas allowlist. See [specs/tech/bridges.md](specs/tech/bridges.md) and [specs/tech/sponsored-gas.md](specs/tech/sponsored-gas.md).

- Remaining recurring-pledges work is operational: deploy the updated contracts to testnet, regenerate `deployments/base-sepolia.env`/`render.yaml`, copy/fund the scheduler key, set `RECURRING_PLEDGE_SCHEDULER_ENABLED=true`, redeploy workers, and verify a due pledge produces a `StandingPledgeExecuted` event through the indexer.

- **(Trust)** Make `scripts/local-ui-gateway.mjs` proxy `/api/*` to the local indexer (`http://localhost:42069`). Today it only proxies to IPFS, so the eight-domain local entry point at `http://<domain>.localhost:8088` serves the IPFS build with no runtime `config.json`; the SDK falls back to a relative `/api/events`, every request 404s, and *every* data-backed page (browse projects, explore causes, creator dashboard, channel lists) renders an error or empty state. The product looks catastrophically broken and is not. Found 2026-07-25 while verifying [specs/product/use-cases.md](specs/product/use-cases.md); the workaround is to use the Vite dev server with an explicit `VITE_EVENT_CACHE_URL`, but the `:8088` path is what README/inbox tell reviewers to open.

- **(Trust)** Fix funding-amount units on the Aligning cause board. The same project renders `US$0.7 USDZZZ / US$3.79 USDZZZ` on its LazyGiving page and `0.0000000000007 / 0.00000000000379 ETH` on the cause board — raw token decimals plus a wrong denomination label. Reproduce: seed with `--seed=demo`, open the cause board for `bafkreiblpyudjm6o7fke7cszndm5fcbv5ob5slmdw3tjeuqxj7y5msuige`, compare against the Bridge-Building Workshop Series project page.

- **(Trust)** Fix mojibake on the content-funding channel page: the content-items explainer renders `âTrusted attestedâ ... âUncoveredâ` where curly quotes were intended. Note there is already a `review.copy-encoding` verifier check — work out why it didn't catch this and extend it if the gap is real.

- **(Tell)** `/explore` on Aligning promotes causes whose cause boards are empty at every trust scope, including "Anyone", with no signal on the card. In the demo seed the 19 alignment attestations point at 13 statements, none of which are the causes Explore surfaces. Decide whether this is a seed-data problem, a ranking problem (sort/flag causes by aligned-project count), or both — a newcomer's first click currently lands on "No aligned projects yet."
