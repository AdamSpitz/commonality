# To Do

This is the project's inbox; use this for tasks that might be suitable for an LLM to do.

If you have stuff that needs human attention, you can put it in [Adam's inbox](/inbox.md) instead. See [task autonomy tiers](/workflow/task-tiers.md).

When an item from this page is done and no longer needs an LLM implementor's attention, don't mark it "done", just delete it. I don't want this file to get cluttered with already-completed items.

----

- [ ] **(Tell)** Build the contribution sequencing UI/service for the no-custody on-ramp path: start contribution, create on-ramp session, detect USDC arrival, handle allowance if needed, send `buyERC1155` from the user's wallet, show confirmation/retry/error states, and connect the result to leaderboard/status display. See [specs/tech/bridges.md](specs/tech/bridges.md). (Progress 2026-07-06: the "handle allowance if needed" piece is done at the SDK layer — `buyProjectTokens`/marketplace buys now read the ERC20 allowance and skip the `approve` tx when it already covers the cost, so repeat contributions and the sponsored-gas path don't eat a redundant UserOp. Progress 2026-07-09: the on-ramp leg is now unblocked — Coinbase Onramp is ratified and the load-bearing "delivers USDC to a counterfactual 4337 address" question is confirmed GO. The [`spikes/coinbase-onramp/`](/spikes/coinbase-onramp/) harness has working reference code. Progress 2026-07-09 later: the platform API now has the real on-ramp service endpoints: `POST /onramp/coinbase/session` mints a Coinbase Onramp URL for Base USDC into the donor wallet, and `GET /onramp/base-usdc-balance?address=…` polls Base native-USDC balance plus address-deployed status for arrival detection. Progress 2026-07-09 later still: the UI now has a typed `ui/src/lazy-giving/onrampClient.ts` for those endpoints, including missing-config and API-error handling. Progress 2026-07-09 latest: the visible UI now has a first card/on-ramp state-machine slice in `BuyTokensSection`: start Coinbase checkout via the typed client, persist/reopen the checkout URL, manually check Base USDC arrival, and show loading/success/error states with focused component tests. Remaining: Privy/Pimlico embedded-wallet login/signing, sponsored `buyERC1155`, and deeper confirmation/leaderboard status once the sponsored transaction path is live. Progress 2026-07-09 latest: card/on-ramp checkout now starts automatic Base USDC balance polling, gates the Give button while the checkout-linked wallet has less than the typed contribution amount, and ungates it once enough USDC is detected. Progress 2026-07-09 latest: disconnected card contributors now see an inline sign-in/wallet CTA in the Pay by card flow so the checkout has a non-custodial destination address before Coinbase Onramp starts. Progress 2026-07-09 latest: the credential blockers for the Privy/Pimlico legs are cleared — Privy app configured (Kernel smart wallets on Base) and wired into the UI, Pimlico + Coinbase CDP keys documented; see [workflow/privy-pimlico-setup.md](workflow/privy-pimlico-setup.md). Progress 2026-07-09 latest: direct and delegatable-note contribution success messages now explicitly tell the donor that project totals and the contributor leaderboard are refreshing from the indexer, with a manual `Refresh status` retry action if the indexer lags. Progress 2026-07-09 latest: the UI now maps Pimlico bundler/paymaster env into Privy's Kernel smart-wallet config when those URLs are present, so the next contribution-sequencing step can exercise Privy smart-wallet UserOps instead of plain embedded-wallet wagmi transactions. Progress 2026-07-09 latest: the Pay by card box is now shown only for USDC-funded projects, matching the Coinbase Base-USDC on-ramp path and avoiding bogus ETH/USDC balance gating on legacy ETH projects. Progress 2026-07-09 latest: on-ramp balance checks now distinguish partial funding from enough-to-give confirmation, showing the detected USDC plus the required contribution amount instead of presenting every poll as a success. Progress 2026-07-09 latest: Coinbase checkout links are now persisted per project+wallet so a donor can reload or navigate back and still reopen an in-flight on-ramp session. Progress 2026-07-09 latest: after the donor successfully sends the onchain USDC contribution, the UI clears that in-flight checkout link and balance-gating state so a completed card flow no longer looks resumable. Progress 2026-07-09 latest: card checkout is now gated on an exact available contribution allocation, so donors cannot on-ramp a snapped/unsupported amount that the later Give step would reject. Progress 2026-07-09 latest: post-contribution success now schedules two automatic project-status refresh retries (in addition to the manual Refresh status action), so totals/leaderboard catch up without making the donor repeatedly click while the indexer lags. Progress 2026-07-09 latest: restored in-flight Coinbase checkout links now trigger an immediate quiet USDC balance check as soon as the donor re-enters an exact contribution amount, instead of waiting for the first 10s polling tick. Progress 2026-07-09 latest: the automatic USDC-arrival poll now pauses while the browser tab is hidden (donors usually finish Coinbase checkout in another tab) and fires an immediate re-check when they return, avoiding wasted API/RPC calls and shaving up to 10s off arrival detection. Progress 2026-07-09 latest: switching wallets (or projects) now clears the previous wallet's detected-USDC balance/status/error so a stale "enough USDC has arrived" reading can't carry over to a different, unfunded wallet. Progress 2026-07-09 latest: an in-flight USDC balance check is now also discarded if the donor switches wallets before it resolves, so a late-landing check for the previous wallet can't repopulate the new wallet's balance/status/error. Progress 2026-07-10: restored Coinbase checkout URLs are now validated against the expected `https://pay.coinbase.com` host before being rendered/opened, so a tampered `localStorage` value can't inject a `javascript:`/foreign-host link into the Reopen-checkout action. The same `https://pay.coinbase.com` host check now also gates the freshly-minted session URL before it is opened/persisted, so a misconfigured/compromised payments API returning a foreign checkout link is rejected instead of opened. Progress 2026-07-10 later: the automatic USDC-arrival poll is now bounded — it stops after ~5 minutes (30 attempts at the 10s cadence) instead of hitting the platform API + Base RPC forever, and shows a "Stopped checking automatically… use Check USDC arrival" prompt; the manual button and tab-return re-check re-arm the window, and it resets on wallet/project switch and on each fresh checkout. Progress 2026-07-10 latest: the **sponsored `buyERC1155` leg is now confirmed working live** on Base Sepolia — `useWriteClients` routes through the Privy Kernel smart-account client, and `buyProjectTokens` batches approve+buy into one Pimlico-sponsored ERC-7579 UserOp (mined tx `0xf59d2d4a…`, donor paid 0 gas). Remaining on this item is mostly polish/integration on top of that now-working path; the underlying wallet/signing/sponsorship machinery is proven.)

- [ ] **(Tell)** Run the Privy + Pimlico embedded-wallet spike (provider is ratified — Privy, 2026-06-18 — this is feasibility, not a provider re-eval). Confirm the `[confirm in spike]` items in [specs/tech/bridges.md](specs/tech/bridges.md): (1) ✅ **DONE 2026-07-09 (GO)** — the chosen on-ramp (Coinbase, now ratified) delivers USDC to a *counterfactual* (undeployed) EIP-4337 account address, confirmed live on Base mainnet; the eager-deploy fallback is not needed. Harness: [`spikes/coinbase-onramp/`](/spikes/coinbase-onramp/). (2) Pimlico+Privy follow the `initCode`-on-first-UserOp deploy pattern and the paymaster sponsors that deploy-inclusive first op; (3) Privy key export works against a real account (pre-mainnet gate); (4) login/recovery modal UX holds the walletless framing end-to-end; and sanity-check per-wallet/MAU economics before mainnet. See also [specs/tech/sponsored-gas.md](specs/tech/sponsored-gas.md) §1. **Progress 2026-07-09: accounts are now set up, so this spike is runnable. Progress 2026-07-09 latest: Pimlico bundler/paymaster URLs now flow through `scripts/setup-env.sh` into `ui/.env` and enable Privy's Kernel smart-wallet config in the UI when present.** The Privy app is configured (email login; **Kernel** smart wallets on **Base**; auto-create embedded wallets on login; allowed origins for localhost:8088 + `*.testnet.commonality.works`), and `VITE_PRIVY_APP_ID` flows through `setup-env.sh` into `ui/.env` so the UI is in Privy mode. Pimlico API key and the Coinbase Onramp (CDP) key (reused from the on-ramp spike) are documented and wired. Setup steps + the smart-wallet-type rationale: [workflow/privy-pimlico-setup.md](workflow/privy-pimlico-setup.md). Progress 2026-07-10: added a step-by-step in-browser runbook for this spike — [`spikes/privy-pimlico/README.md`](/spikes/privy-pimlico/README.md) — with the automated-vs-human split, the exact click path, how to capture the Pimlico UserOp trace, and a per-item PASS/FAIL table. It flags the load-bearing prerequisite that the contribution path still signs from the embedded EOA (`useWriteClients` → wagmi `useWalletClient`; no `useSmartWallets` usage yet), so the Privy smart-wallet client must be wired before any UserOp reaches Pimlico and items (2)/(3) become observable. Progress 2026-07-10 later: initial smart-wallet client wiring is in place — `PrivyAppProvider` now mounts `SmartWalletsProvider`, `useWriteClients` prefers the Privy Kernel smart-account client when Privy + a bundler URL are configured, and `permissionless` is an explicit UI dependency. Progress 2026-07-10 latest: live browser spike partially ran on Base Sepolia; see `CONTINUITY.md` for full details. A real UserOp was observed with non-empty `factory`/`factoryData` (so counterfactual first-op deploy is effectively confirmed), but it was routed to `https://public.pimlico.io/v2/8453/rpc` with no paymaster and failed `AA21 didn't pay prefund`. After forcing `getClientForChain({ id: 84532 })`, the UI returned “Wallet is not ready,” so the remaining blocker is why Privy is not producing a Base Sepolia smart-wallet client using the configured Pimlico bundler/paymaster (check Privy dashboard network config / client config shape). **Progress 2026-07-10 latest: RESOLVED and items 1–3 PASS.** Root cause of the "Wallet is not ready" / mainnet-routing was that Privy reads smart-wallet networks (bundler/paymaster/chain) from the **Privy dashboard**, not from the code-level `PrivyProvider` config — the `configuredNetworks` we passed in code was silently ignored. After adding Base Sepolia (84532) with the Pimlico bundler+paymaster in the dashboard (and removing the dead code config), a real contribution UserOp routed to `api.pimlico.io/v2/84532/rpc` with a populated Pimlico paymaster and non-empty `factory`/`factoryData`. Item 1 (counterfactual inline deploy) and item 2 (paymaster sponsors first op) confirmed live (mined tx `0xf59d2d4a…`, smart wallet `0xe16d…` now deployed). Item 3: real Kernel v3 calldata is **ERC-7579 `execute(bytes32,bytes)`**, so `CreatorGasTank` was retargeted (see item below). Also fixed an `ERC20InsufficientAllowance` revert by batching approve+buy into a single UserOp for smart accounts in the SDK. **Remaining: items 4 (Privy key export) and 5 (login/recovery modal UX) — both human-only — plus the per-wallet/MAU economics sanity check. These need a human (browser + judgment), not an LLM; they are written up for Adam in [inbox.md](/inbox.md). A fresh LLM should not try to code items 4/5.**

- [ ] **(Tell)** Finish sponsored-gas support. Initial `CreatorGasTank` contract spike is done (per-creator ETH tanks, anyone-can-fund, self-enrollment, configurable per-wallet caps, SimpleAccount-shaped validation, `postOp` debit, unit tests). Remaining: confirm the Kernel decoder against a real Privy+Pimlico UserOp trace; tune production caps from real UserOp overhead; deploy to testnet and add bundler/UI wiring plus behavioral verifier monitoring; implement `GasTankFunder` USDC→ETH swap adapter. See [specs/tech/sponsored-gas.md](specs/tech/sponsored-gas.md). (Progress 2026-07-09: smart-account implementation is now chosen — **Kernel** (via Privy, on Base) — so the current SimpleAccount-shaped decoder must be confirmed/retargeted against Kernel's `execute`/`executeBatch` calldata format. See [workflow/privy-pimlico-setup.md](workflow/privy-pimlico-setup.md). Progress 2026-07-09 latest: `CreatorGasTank` now also decodes Kernel `execute(address,uint256,bytes,uint8)` and `executeBatch((address,uint256,bytes)[])`, with unit coverage for sponsored buy/approve calls. Still needs live trace confirmation. Progress 2026-07-09 latest: `testnet.sponsored-gas` now does behavioral read-only monitoring too: after bytecode checks it calls `settlementToken()`, `entryPoint()`, `sponsorshipConfig()`, and `minSponsoredContributionAmount()` and fails if deployed config drifts from `deployments/base-sepolia.env`. Progress 2026-07-09 latest: `CreatorGasTank` now rejects malformed account/inner calldata with explicit custom errors before selector decoding, with focused Hardhat coverage. Progress 2026-07-09 latest: approval-only sponsored UserOps are now rejected; approvals must be batched with a buy/refund primary action so a wallet cannot drain a creator gas tank by repeatedly sponsoring harmless approvals. Progress 2026-07-09 latest: added `testnet.sponsored-gas-abuse`, a guarded verifier monitor that scans recent `CreatorGasTank` funding/debit events for tank-drain totals, debit-to-funding ratio, and per-wallet debit-count anomalies. Progress 2026-07-09 latest: the sponsored-gas deployed-config and abuse monitors now feed `facet.security`, so the security dashboard will surface sponsored-gas deployment/config drift and tank-drain anomalies instead of leaving them as isolated manual checks. Progress 2026-07-10: **Kernel decoder confirmed against a real Privy+Pimlico UserOp and retargeted.** The live Kernel v3 account emits ERC-7579 `execute(bytes32 mode, bytes executionCalldata)` (`0xe9ae5c53`), NOT the Kernel v2 `execute(address,uint256,bytes,uint8)` shape originally coded — real ops would have reverted `UnsupportedAccountCall`. `CreatorGasTank._validateAccountCall` now decodes ERC-7579 single mode (packed `target|value|callData`) and batch mode (`abi.encode(Execution[])`), with Hardhat coverage for both; SimpleAccount support retained. Remaining: tune production caps from real UserOp overhead; deploy paymaster to testnet + bundler/UI wiring; implement `GasTankFunder`.)

- [ ] **(Tell)** Finish embedded-wallet failed-project refund support: wire sponsored gas where appropriate, then verify the existing refund UX against a Privy embedded wallet on testnet. The UI already detects refundable positions, calls `refundERC1155`, links the transaction, explains that refunded USDC returns to the user's wallet, and offers next steps (keep USDC, re-contribute, or use a licensed off-ramp/KYC flow). See [specs/tech/bridges.md](specs/tech/bridges.md). (Progress 2026-07-09 latest: refund UX now fails visibly when the donor is not signed in or the wallet client is not ready, and successful refunds explicitly say indexer status is refreshing with a manual `Refresh status` retry action. Progress 2026-07-10: refund failures are now run through `humanizeTxError`, so a wallet-cancelled or out-of-gas refund shows the same calm, actionable message the buy/secondary-market flows use instead of a raw multi-line wallet/RPC dump; unrecognized reverts still fall through to their raw reason.)

- Remaining recurring-pledges work is operational: deploy the updated contracts to testnet, regenerate `deployments/base-sepolia.env`/`render.yaml`, copy/fund the scheduler key, set `RECURRING_PLEDGE_SCHEDULER_ENABLED=true`, redeploy workers, and verify a due pledge produces a `StandingPledgeExecuted` event through the indexer.

- [ ] Hardhat 2→3 migration — defer until after current testnet stabilization, but revisit before mainnet. Treat as a standalone migration project, not a dependency bump.

- [ ] Verify the Render/Ponder deploy fix over a few normal indexer redeploys: `commonality-indexer` now has a tiny persistent disk so Render should do stop-before-start deploys instead of rolling deploys, avoiding Ponder `DATABASE_SCHEMA` lock conflicts. If lock failures recur, split the indexer into a singleton writer/worker plus a separately deployed read-only web/API service. See [workflow/deployment.md](workflow/deployment.md#known-render-indexer-deployment-trap-ponder-schema-lock).


