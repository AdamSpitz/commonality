services:
  # Hardhat local blockchain node
  hardhat-node:
    build:
      context: ./hardhat
      dockerfile: Dockerfile
    container_name: commonality-hardhat-node
    ports:
      - "8545:8545"
    volumes:
      # Mount project root so deploy script can write .env files
      - .:/workspace
    working_dir: /workspace/hardhat
    command: npx hardhat node --hostname 0.0.0.0
    healthcheck:
      test: |
        curl -f -X POST -H "Content-Type: application/json" \
        --data '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}' \
        http://localhost:8545 || exit 1
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 5s
    networks:
      - commonality

  # One-shot service to deploy contracts
  hardhat-deploy:
    build:
      context: ./hardhat
      dockerfile: Dockerfile
    container_name: commonality-hardhat-deploy
    volumes:
      # Mount project root so deploy script can write .env files
      - .:/workspace
    working_dir: /workspace/hardhat
    command: npx hardhat run scripts/deploy-local.js --network localhost
    depends_on:
      hardhat-node:
        condition: service_healthy
    environment:
      # Point to the hardhat-node service using Docker networking
      - HARDHAT_NETWORK_URL=http://hardhat-node:8545
    networks:
      - commonality

  # Local IPFS node for testing
  ipfs:
    image: ipfs/kubo:latest
    container_name: commonality-ipfs
    ports:
      - "5001:5001"  # API port
      - "8080:8080"  # Gateway port
    volumes:
      # Use ephemeral storage - fresh IPFS node each run
      - ipfs-data:/data/ipfs
    # Custom entrypoint to configure IPFS for path-based gateway (no subdomain redirects)
    # This prevents the gateway from redirecting to CID.ipfs.localhost which doesn't resolve
    entrypoint: >
      sh -c "
      if [ ! -f /data/ipfs/config ]; then ipfs init --profile=test; fi &&
      ipfs config --json Gateway.PublicGateways '{\"localhost\": {\"Paths\": [\"/ipfs\", \"/ipns\"], \"UseSubdomains\": false}}' &&
      ipfs daemon --migrate=true
      "
    healthcheck:
      test: ["CMD-SHELL", "ipfs id || exit 1"]
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 5s
    networks:
      - commonality

  # Ponder indexer service
  indexer:
    build:
      context: ./indexer
      dockerfile: Dockerfile
    container_name: commonality-indexer
    ports:
      - "42069:42069"
    volumes:
      # Mount project root to access .env file
      - .:/workspace
    working_dir: /app
    depends_on:
      hardhat-node:
        condition: service_healthy
      ipfs:
        condition: service_healthy
      hardhat-deploy:
        condition: service_completed_successfully
    environment:
      # Point to hardhat-node service
      - PONDER_RPC_URL_31337=http://hardhat-node:8545
      # Use ephemeral in-memory database for tests (fresh state each run)
      - PONDER_EPHEMERAL=true
      # Point to local IPFS gateway
      - IPFS_GATEWAY=http://ipfs:8080/ipfs
      - IPFS_API=http://ipfs:5001
    # Load contract addresses from .env file
    env_file:
      - .env
    healthcheck:
      test: |
        curl -f -X POST -H "Content-Type: application/json" \
        --data '{"query":"{ _meta { block { number } } }"}' \
        http://localhost:42069/graphql || exit 1
      interval: 2s
      timeout: 10s
      retries: 30
      start_period: 10s
    networks:
      - commonality

  # Integration test runner (use with `docker-compose run test-runner`)
  test-runner:
    build:
      context: .
      dockerfile: ./integration-tests/Dockerfile
    volumes:
      # Mount only integration test source for live updates
      # SDK source is baked into the image to work with Linux node_modules
      - ./integration-tests/src:/workspace/integration-tests/src
      - ./integration-tests/tsconfig.json:/workspace/integration-tests/tsconfig.json
      - ./integration-tests/.mocharc.json:/workspace/integration-tests/.mocharc.json
      # Mount .env for contract addresses
      - ./.env:/workspace/integration-tests/.env
    working_dir: /workspace/integration-tests
    depends_on:
      indexer:
        condition: service_healthy
    environment:
      # Tests connect to services via Docker network
      - HARDHAT_NETWORK_URL=http://hardhat-node:8545
      - INDEXER_URL=http://indexer:42069
      - IPFS_GATEWAY=http://ipfs:8080/ipfs
      - IPFS_API=http://ipfs:5001
      # Allow passing test pattern via environment
      - TEST_PATTERN=${TEST_PATTERN:-}
    command: sh -c "pnpm test ${TEST_PATTERN}"
    networks:
      - commonality

networks:
  commonality:
    driver: bridge

volumes:
  ipfs-data:
